"""SECRSI sector snapshot — stub.

Will compute the 11:00 IST snapshot of per-stock %chg-from-open and
aggregate to per-sector median scores via SectorMapper. Spec §3.1.

PRE_REGISTERED 2026-04-27; implementation follows in TDD commits.
"""
from __future__ import annotations


def take_snapshot(*args, **kwargs):
    raise NotImplementedError("SECRSI sector_snapshot.take_snapshot — TDD build pending")
